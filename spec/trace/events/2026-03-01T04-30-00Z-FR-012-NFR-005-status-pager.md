# Trace Event: FR-012 / NFR-005 — status --fail-if-dirty, $PAGER, --no-prompt

timestamp: 2026-03-01T04:30:00Z
agent: copilot
requirements: [FR-012, NFR-005]

## Changes

- Added `--fail-if-dirty` flag to `StatusArgs`; returns `Err` with dirty count when N entries differ (FR-012).
- Added `--no-prompt` flag to `StatusArgs` for CI/automation flows (FR-005 partial).
- Added `get_password_no_prompt()` to `src/crypto/mod.rs` for non-interactive password resolution.
- Added `print_with_pager()` to `src/cli/status.rs` and `src/cli/diff.rs`; pipes human-readable output through `$PAGER` when set (NFR-005).
- Updated `run_authenticated` to use `get_password_no_prompt` and buffer output for pager.

## Verification

- All existing tests pass (`cargo test`).
- New unit tests: `fail_if_dirty_clean_vault_returns_zero_dirty`, `fail_if_dirty_modified_file_returns_dirty`, `fail_if_dirty_without_password_source_is_error`, `print_with_pager_no_pager_set_does_not_panic`, `get_password_no_prompt_fails_without_source`, `get_password_no_prompt_uses_env_var`.
