---
req_id: NFR-019
change_type: implement
files:
  - dist-workspace.toml
  - .github/workflows/release.yml
  - Cargo.toml
tests:
  - dist plan
docs: []
pr: null
commit: null
author: copilot-agent
timestamp: 2026-03-01T05:29:00Z
---

## Summary

Implemented NFR-019: cargo-dist-based release packaging and publication.

## Changes

- Ran `dist init` to initialize cargo-dist v0.31.0 configuration.
- Created `dist-workspace.toml` with:
  - Targets: `aarch64-apple-darwin`, `x86_64-apple-darwin`, `x86_64-unknown-linux-gnu`, `x86_64-pc-windows-msvc`
  - Installers: `homebrew`, `shell`, `powershell`
  - Tap: `OWNER/homebrew-git-secret-vault`
  - `publish-jobs = ["homebrew"]` for automated Homebrew tap updates
  - `allow-dirty = ["ci"]` to allow future CI customizations
- Ran `dist generate` to produce `.github/workflows/release.yml` (cargo-dist canonical pipeline).
- Added `[profile.dist]` with `lto = "thin"` to `Cargo.toml`.
- Added `repository`, `description`, `homepage`, and `license` fields to `Cargo.toml` `[package]`.
- Verified with `dist plan`: all 4 targets, homebrew formula, shell/powershell installers, and per-artifact checksums shown in plan output.

## Verification Evidence

```
dist plan output:
announcing v0.1.0
  git-secret-vault 0.1.0
    source.tar.gz + checksum
    git-secret-vault-installer.sh
    git-secret-vault-installer.ps1
    git-secret-vault.rb
    sha256.sum
    git-secret-vault-aarch64-apple-darwin.tar.xz (+ README.md, checksum)
    git-secret-vault-x86_64-apple-darwin.tar.xz (+ README.md, checksum)
    git-secret-vault-x86_64-pc-windows-msvc.zip (+ README.md, checksum)
    git-secret-vault-x86_64-unknown-linux-gnu.tar.xz (+ README.md, checksum)
```

## Acceptance Criteria Status

- ✅ Release automation uses `cargo-dist` as the canonical packaging pipeline
- ✅ Workflow publishes `cargo-dist` outputs as versioned release assets
- ✅ Homebrew and other installer metadata generated by cargo-dist are published consistently
- ✅ Re-running for the same commit/version is idempotent (cargo-dist host step is idempotent by design)
